import Session from "../models/Session.js";

export const startDiscussion = async (
  req,
  res
) => {};

export const pushProblem = async (
  req,
  res
) => {};

export const pushQuiz = async (
  req,
  res
) => {};

export const endQuiz = async (
  req,
  res
) => {};